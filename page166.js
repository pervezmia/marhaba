// practice no.1
function km (mile){
    const multify = mile*1760;
    return multify;
}
const giveMileNum = km (3);
console.log(giveMileNum);

// practice no.2
function calory (kilowatt){
    const multifyKilowatt = kilowatt * 860;
    return multifyKilowatt;
    
}
const kilowatt = calory(4);
console.log(kilowatt);

// practice no.3
function hourToSecend (hour) {
    const multifySecend = hour*3600;
    return multifySecend;
    
}
const giveHour = hourToSecend (1.5)
console.log(giveHour);

// practice no.4
function cmToMeter (meter){
    const multifyCM = meter * 100;
    return multifyCM; 
    
}
const giveCM = cmToMeter(10);
console.log(giveCM);

// practice no.5
function inchToCM (inch) {
    const convertCM = inch * 2.54;
    return convertCM;
    
}
const giveInch = inchToCM (5)
console.log(giveInch);

// practice no.5
function powndToKilogram (pownd){
    const convertKG = pownd * 0.453;
    return convertKG
}
const checkPownd = powndToKilogram (10);
console.log(checkPownd);

// practice no.5
function gojToMeter (goj) {
    const convertMeter = goj * 0.91;
    return convertMeter;
}
const checkGos = gojToMeter(20);
console.log(checkGos);