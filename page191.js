// practice no.1
const num1 = 100;
const num2 = 20;
const total = `The difference between ${num1} and ${num2} is : ${num1-num2}`; 
console.log(total);

// practice no.2

const employee = {
    name: "Pervez",
    age: 24,
    salary: 212211
    }
    const info = `${employee.name} ${employee.age} ${employee.salary}`
    console.log(info);

    // practice no.3
    const fruits = [ 'banana','apple','date'];
    const favFood = `My favourite food is ${fruits[2]}`;
    console.log(favFood);

    // practice no.4
    const a = 50;
    const b = 10;
    const division = `The division of a and b is ${a/b}`;
    console.log(division);

    // practice no.5
    const person = {
        firstName: "Amit",
        lastName: "Kumar"
    }
    const totalName = `Full Name: ${person.firstName} ${person.lastName}`;
    console.log(totalName);

    // practice no.6
    const animals = ['Cat', 'Dog', 'Elephant'];
    const favAnimals = `My favorite animals are ${animals[0]}, ${animals[1]}, ${animals[2]}`
    console.log(favAnimals);
    
    // practice no.7
    const student = {
        name: "Pervez",
        age: 23,
        results: [60,80,70]
    }
    const average = `The student name is ${student.name} and his average result is ${(student.results[0] + student.results[1] + student.results[2])/3 }`;

    console.log(average);