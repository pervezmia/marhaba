    // practice No.1
    
    function somosti1 (fatherAge, myAge){
        const store = fatherAge + myAge;
        console.log(store);
    }
    somosti1 (50,20);
    
    // practice No.2

    function calculator ( input1, input2 ) {
        const sum = input1 + input2;
        console.log(sum);
        const subtrac = input1 - input2;
        console.log(subtrac);
        const multiplication = input1 * input2;
        console.log(multiplication);
    }
    const press = calculator ( 20, 15);


      // practice No.3

    function sum1 (sub1,sub2,sub3) {
        const total = sub1 + sub2 + sub3;
        return total;
    }
    const somosti = sum1 (13,40,30);
    console.log(somosti)
    

              // practice No.4

        function myAge (jonmoSal) {
            let year = 2025;
            let presentMyAge = year-jonmoSal;
            console.log(presentMyAge);
        }
             myAge (2002);

              // practice No.5

              function lawKenarBapar (lawBudget){
                const lawPrice = 35;
                const totalGetlaw = lawBudget / lawPrice;
                console.log(totalGetlaw);
              }
              lawKenarBapar (385);

              // practice No.6

              function total1 ( num1, num2, num3 , num4) {
                    let sum = num1 + num2 + num3 + num4;
                    let ghor = sum / 4;
                    return ghor
              }
              const gor = total1( 5, 8, 9, 10);
              console.log(gor);

              // practice No.7

              function dokandar ( kenaPrice ) {
                    const profit = 250;
                    const total = profit + kenaPrice;
                    console.log(total);               
              }

              let productCost = dokandar (200);


                // practice No.8

              function age ( myBirt ) {
                const year = 100;
                const target100Year = year + myBirt;
                console.log (target100Year);
              }
              let finallyYear = age (1552);

              
              // practice No.9

              function useMobile ( everyDay) {
                    const month = 30;
                    const totalMobileUse = month * everyDay;
                    console.log(totalMobileUse);
              }
              let mobile = useMobile (4);
    
    